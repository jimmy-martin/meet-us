<!-- Footer -->
<footer class="text-center">
    <!-- Grid container -->
    <div class="container p-4">

    </div>
    <!-- Grid container -->

    <!-- Copyright -->
    <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2)">
        © 2020 Copyright:
        {% set textColor = app.session.get('theme') == 'darkly' ? 'white' : 'black' %}
        <a class="text-{{ textColor }}" href="#">Meet Us</a>
        <a href="{{ path('backoffice_toggle_theme') }}" id="theme-btn" class="ms-2 btn btn-primary">
            {{ app.session.get('theme') == 'darkly' ? 'Light Mode' : 'Dark Mode' }}
        </a>
    </div>
    <!-- Copyright -->

</footer>
<!-- Footer -->